# 工程演进记录

记录日期：2026-09-20。目的：保存每次开发当时的判断、证据与局限，让项目既能继续开发，也能回答「什么场景、为什么做、如何做、效果如何、上线还会遇到什么」。

本记录以历史提交与已有验收记录为依据回填；回填不是当时的逐分钟开发日志。风险推演不等于发生过生产事故，测试环境结果不等于线上容量。后续按阶段追加，不用新结论覆盖旧阶段的事实。

## 当前快照

- 路线：Java 21 / Spring Boot 4.0.3 / MyBatis-Plus 3.5.15 / MySQL / Redis / RabbitMQ，DDD 模块化单体。Agent 与水印是业务特色，核心工作仍是事务、持久化、并发正确性、可靠消息与运维。
- 功能基线：`8506a8d` 的 Outbox 已完成本地迁移、重启与接口验收；后续 E07 补齐集成测试适配和隔离。
- 本地运行：持久化记账幂等已应用迁移并联调；新前端仍是面向手机的 Web 验证界面，尚不能视为原生小程序完成。
- 成熟度：工程化 MVP。尚无生产运行、容量基线、真实流量下 P95/P99 或高可用验收结论。
- 最新验证：后端完整 70 项通过，前端 23 项与代理 1 项通过；本地导出状态与快照已核对，尚未验证浏览器下载。

## 阶段索引

| 阶段 | 当时推进的核心问题 | 提交 / 证据 | 达成与边界 |
| --- | --- | --- | --- |
| E00 | 业务闭环与持久化 | `3d13ed6`、`d3e83a0`；现状文档 | 账本与旅行接口接入；仍是 MVP |
| E01 | 旧请求误删后继请求的 Redis 标记 | `7358ba1` | 竞态复现后修复，5 项 Redis 测试通过 |
| E02 | 短期去重无法可靠重放已提交结果 | `679d701` | MySQL 持久化幂等，9 项专项测试通过 |
| E03 | 浏览器链路没有真正传递幂等键 | `ed74169` | 修复代理，实际请求与持久记录对上 |
| E04 | 明确拒绝与结果未知需要不同交互 | `77d61b1` | 可修正校验失败的表单；未知结果保留原请求 |
| E05 | 页面刷新丢失未知结果请求 | `10a41f9` | 同标签页恢复原键和请求；真实断网验收待做 |
| E06 | 导出事务提交与消息发送存在空隙 | `8506a8d` | 当时专项测试通过；部署由 E07 补齐 |
| E07 | 旧集成测试适配、隔离与本地部署 | `95bd06b`；回归及联调 JSON 报告 | 后端 61 项通过；迁移、重启、导出快照验收完成 |
| E08 | 失败导出受控恢复 | `63ef6fe` 与恢复报告 | 后端 64 项通过；重试接口本地部署 |
| E09 | 发布/消费状态可见性 | `bdafaad` 与可见性报告 | 后端 65 项通过；列表接口部署验收 |
| E10 | 消费失败消息保留 | `11b058e` 与转存报告 | 后端 67 项通过；原拓扑兼容部署 |
| E11 | 积压与故障巡检 | `2b46340` 与巡检报告 | 6 项脚本测试通过；真实健康/未知状态验证 |
| E12 | 告警去重与恢复 | `7688407` 与报告 | 13 项脚本测试通过；连续健康采集验证 |
| E13 | Java 可观测性 | 本轮代码与指标报告 | 后端 70 项通过；本地监听及权限边界验收 |

测试数量为各阶段指定范围，不能跨阶段相加为全量测试数。历史验证的完整上下文见[交接记录](development-handoff.md)、[回归计划](regression-plan.md)。

## E00 · 从页面原型走到业务闭环

**当时状态。** 账本已有成员、邀请、临时参与人、审批、结算、凭据和导出等基础能力；新页面需要与这些后端能力对齐。旅行需要独立持久化，也允许关联账本。

**问题与动机。** 页面能展示样例，并不能证明刷新后存在数据、多人操作合法或金额计算正确。求职项目需要能解释业务约束如何落在 Java 和数据库里。

**采取方案。** 沿用 DDD 模块化单体，使用 MyBatis-Plus / MySQL 持久化；金额采用整数分并校验分摊守恒；旅行聚合通过版本更新控制冲突，后端承担所有权校验与规模限制。前端接入接口，支持按日编辑、移动与排序。接通高德地点与步行路线查询；规划先生成规则草案。

**效果与证据。** 基础接口和页面已联调，历史交接记录记载 39 项 Java 测试及当时 8 项前端测试通过。这是历史范围，不是当前分支的全量回归结论。参见[后端现状](backend-readiness.md)、[账本覆盖](ledger-ui-coverage.md)。

**剩余问题。** 真实微信身份、原生小程序迁移、生产部署未完成；图片仍使用本地存储。地图已有缓存和实例内并发限制，但尚不具备全局供应商配额治理。

**企业场景与学习。** 分层的价值在于让领域规则、事务边界、外部 API 各自可替换和验证。面试应能说明：金额为什么不用浮点数；多人修改如何发现冲突；数据库已写入但页面报错会怎样。

## E01 · 修复 Redis 标记所有权竞态

**当时状态。** Redis 幂等防护采用带 TTL 的占位，业务失败后删除标记。

**问题与动机。** 已复现的竞态：请求 A 执行太久，标记过期；B 获取新标记；A 随后失败并删除标记，误伤 B。仅知道 key 存在不能证明自己仍是持有者。

**采取方案与取舍。** 为请求保留 UUID 所有权令牌，用 Lua 原子比较令牌后再删除。清理失败作为 suppressed 异常保留，不能覆盖最初业务异常。保持现有技术栈，先修复正确性；不把引入锁框架等同于解决全部并发问题。

**效果与证据。** 修复前过期接力用例失败，修复后 5 项真实 Redis 测试通过，覆盖并发占位、失败重试、过期接力及清理异常；与 6 项旅行领域测试一起验证，共 11 项。提交 `7358ba1`。

**剩余问题。** TTL 去重无法永久防重，锁租期也不能替代数据库约束。账本锁的超时、续租或 fencing 仍需单独评估。

**企业场景与学习。** 慢请求、长 GC 或外部服务阻塞都可能让租期过期。面试重点：为什么 GET 后 DEL 两条命令不够；解锁为什么必须校验所有者；分布式锁保护与数据库最终正确性的关系。

## E02 · 将记账幂等落入 MySQL 事务

**当时状态。** Redis 防护已修复所有权，但请求超时后，客户端无法可靠知道是否已经记账；TTL 过后重试仍可能产生重复账单。

**问题与动机。** 这是设计风险，并非已有生产重复账单事故。目标是同一业务请求在重试时返回原结果，而不是简单提示「重复请求」。

**采取方案与取舍。** 以用户、账本、操作和请求键组成唯一范围；对规范化 JSON 计算 SHA-256 摘要，同键不同内容拒绝。通过唯一约束与行锁串行化竞争，把幂等记录、账单写入和响应快照置于同一个数据库事务；重放前仍检查当前权限。使用 JdbcTemplate 与 MyBatis 共用数据源和事务，不依赖进程内锁。暂保留缺少请求头的旧客户端 Redis 路径。

**效果与证据。** 9 项真实隔离 MySQL 测试覆盖新实例重放、事务回滚、6 个并发请求只产生一份业务效果、摘要冲突、范围隔离和序列化等；加上 Redis 5 项、旅行领域 6 项，共 20 项通过。随后迁移 004 已在本地应用。提交 `679d701`；设计见[持久化幂等](durable-idempotency.md)。

**剩余问题。** 无请求键旧客户端不具备同等保证；键的保留期限不能随意缩短，否则旧请求可能重新执行。数据库锁等待、连接池压力和事务超时仍需容量测试；配置事务超时不意味着完整 HTTP 链路硬超时。

**企业场景与学习。** 弱网重试、客户端重复点击、服务实例重启。重点解释唯一约束、事务传播、行锁及结果重放如何配合；不要将业务效果防重描述为网络请求只发送一次。

## E03 · 用真实链路发现代理丢请求头

**当时状态。** 后端幂等测试通过，前端也生成了请求键，但浏览器操作后新增的持久幂等记录数量不符合预期。

**问题与动机。** 这是本地真实联调发现的问题。代理的请求头白名单没有转发 `Idempotency-Key`，使浏览器实际退回旧逻辑。仅测试后端或前端 API 包装均无法覆盖这一跳。

**采取方案。** 修复本地代理白名单，增加真实 HTTP 代理转发测试，核对请求头和请求体。重启代理后，通过浏览器再次提交并检查账单与幂等记录。

**效果与证据。** 测试账本 73 中，修复后浏览器提交的 4.56 元账单有对应持久幂等记录。该账本包含联调过程的 4 笔账单，不能把这些测试操作误判成同键重复写入。见[实际联调证据](idempotency-live-result.json)，提交 `ed74169`。

**剩余问题。** 此次并未覆盖真实浏览器的断网后重试。后续部署若加入网关、反向代理，需要保留同样的端到端检查。

**企业场景与学习。** 单层正确不等于整条链路正确。定位应追踪请求键是否到达服务、是否落库，而不仅观察页面成功提示；生产可用 traceId 关联链路，但避免记录敏感账单正文。

## E04 · 区分明确拒绝和结果未知

**当时状态。** 前端保留请求用于重试，但如果后端明确拒绝参数，也一直保留旧请求，就妨碍用户修正表单。

**问题与动机。** 两种状态需要不同处理：明确未被接受可以编辑；网络超时或其他不确定错误可能发生在提交之后，贸然生成新键可能重复记账。

**采取方案。** 统一提交入口；仅在约定的明确校验失败 `4001` 下清除待重试请求。网络错误及其他未证明未提交的状态保留原键和原请求体。对错误语义采用保守处理，不把所有失败都视为可安全重新创建。

**效果与证据。** 前端累计 17 项测试和 1 项代理测试通过。通过代理对账本 74 验证：错误分摊无账单，同键修正后成功，再次重放仍一笔，更改内容被拒。见[共享账本联调证据](shared-idempotency-live-result.json)，提交 `77d61b1`。

**剩余问题。** 此验收为单成员共享账本，不代表完整多人审批与结算通过。错误码「明确未提交」的含义需要后端持续保证。

**企业场景与学习。** API 错误分类会影响资金类业务正确性；用户看到失败，不代表事务回滚。前后端必须共享可重试与可修改的契约。

## E05 · 页面刷新后恢复待确认请求

**当时状态。** 待重试请求只在内存中，刷新页面会丢失原键和请求体。

**问题与动机。** 服务已提交而响应丢失时，刷新后新建请求可能重复记账。需要在发送前保存恢复材料。

**采取方案与取舍。** 按用户与账本隔离，将原键和请求快照先写入 sessionStorage，再发请求；恢复时校验操作白名单与账本路径，禁止新请求覆盖待确认请求。页面显示待确认金额并提供原请求重试。成功或明确校验拒绝后清理；存储失败则在发送前停止。

**效果与证据。** 新增 6 项测试，前端累计 23 项与 1 项代理测试通过，覆盖重新创建 API 实例后的恢复、隔离、存储异常、覆盖阻止与篡改路径。提交 `10a41f9`。

**剩余问题。** 只覆盖同一标签页刷新，不保证关闭标签页、跨设备或离线长期恢复。尚未进行真实浏览器断网—刷新—重试演练；凭据上传的部分成功也需单独处理。

**企业场景与学习。** 正确性是客户端状态机与服务端事务共同形成的。先存再发的顺序、恢复信息的隔离和清理条件，都应进入回归用例。

## E06 · 导出任务引入事务 Outbox

**当时状态。** 导出任务创建与 RabbitMQ 消息发送存在提交后空隙。若数据库提交后、消息发送前进程退出，任务可能一直等待。

**问题与动机。** 这是故障窗口分析，不是已发生的线上丢单。目标是让「需要发送消息」本身具有持久记录，同时处理 RabbitMQ 不可用与消息无法路由。

**采取方案与取舍。** 在创建导出任务的同一事务写 Outbox；独立扫描器使用 `FOR UPDATE SKIP LOCKED` 领取待发送记录，等待 publisher confirm，检查 mandatory return，成功后置为 SENT。失败指数退避，最多 8 次后 DEAD。消息持久化，保留业务标识；消费者沿用行锁和成功状态防止重复业务效果。选择至少一次投递，允许确认后数据库回滚导致重投。

当前实现每次扫描处理一条、默认间隔 1 秒，发布确认期间持有数据库事务；实现直观，但吞吐与连接占用有代价。未来只有在测量证明瓶颈时，再设计租约领取、批处理与并发工作器。

**效果与证据。** 新增 6 项真实 MySQL 测试与 2 项真实 RabbitMQ 测试，覆盖事务回滚、扫描器重建、退避、DEAD、并发跳过锁行、发布后回滚导致重发，以及成功路由和无法路由。加上已有 20 项，共 28 项指定范围通过。提交 `8506a8d`；详见[Outbox 设计](export-outbox.md)。

**剩余问题。** 迁移 005 尚未应用到运行服务，尚未重启联调；历史等待任务补偿、DEAD 管理入口、DLQ、告警、清理策略及真实进程终止演练未完成。该轮未重跑完整账本集成回归。确认等待 5 秒不是端到端超时保证。

**企业场景与学习。** 数据库和消息代理是两个提交边界。Outbox 解决发送意图不丢，消费幂等处理重复；DEAD 可见不等于有人处理。下一步需要把技术机制延伸到可观测、告警与人工恢复。

## E07 · 让回归测试与实际部署跟上 Outbox

**当时状态。** E06 的专项测试通过，但旧业务集成测试依赖自动发布，而测试 profile 已关闭 Outbox 扫描；运行服务也仍是旧版本。

**问题与动机。** 代码检查发现测试只等待导出快照，并未触发 relay，会无法得到结果。这是检查发现的测试适配缺口，不伪称已经运行失败。同时，单独隔离数据库还不够：若测试和本地服务共享业务队列，本地消费者可能拿到测试任务 ID，在错误数据库处理。

**采取方案与取舍。** 测试显式创建真实 relay 并驱动一次发布，保留 RabbitMQ 与真实消费者处理；没有直接调用业务方法绕过消息边界。完整回归使用独立数据库、消息队列/交换机及 Redis 前缀，并把消息隔离补入统一回归脚本。测试调度保持关闭，使执行时机可控。

**效果与证据。** Java 21 构建镜像中完整 Maven 测试 61 项通过，失败/错误/跳过均为 0；见[分测试类报告](backend-regression-20260920.json)。前端 23 项与代理 1 项通过。运行库先备份，再应用增量迁移 005 并重启，健康检查通过。通过 4178 代理创建导出任务 12，PENDING → SUCCESS，Outbox SENT、发布 1 次；快照与原账本均为 4 笔，源数量未变，见[联调报告](outbox-live-result.json)。单次约 0.555 秒的观察不构成性能结论。

**运行与恢复。** 本地备份位于 `/tmp/trip-ledger-before-outbox-20260920.sql`，权限 0600，不提交仓库；这是临时备份，不是正式备份制度。扫描故障可用 `app.export.outbox.enabled=false` 停止 relay，保留待发送记录；本轮没有做恢复备份或旧版本回滚演练。隔离回归库及对应消息资源保留用于排查，后续按测试资源清理流程处理。

**剩余问题。** 验收对象是持久 JSON 导出快照，不是文件下载或浏览器端 PDF 生成。尚需 DEAD 运维与告警、消费失败恢复、历史等待任务补偿、真实故障演练、可重复初始化与测试资源自动回收。测试源代码仍有按需 DDL，后续应纳入统一迁移管理。

**企业场景与学习。** 新机制上线时必须同步更新旧回归路径；集成环境隔离包括数据库、缓存和消息通道。部署验收应核对中间状态及业务结果，不能只看 HTTP 200 或消息 SENT。

## E08 · 失败导出的受控恢复

**当时状态。** E07 已部署 Outbox，但 DEAD 需要手工改表，消费失败也没有调用方可用的恢复入口。

**问题与动机。** 暂时故障修复后，任务仍可能停在终态；直接重建任务容易丢失原任务关联。无权限控制的重投又可能绕过成员退出或账本所有者变化。

**采取方案与取舍。** 新增原任务重试接口，检查当前成员、原发起者和汇总导出所有者权限。按导出记录 → Outbox 顺序加行锁，同事务恢复 DEAD 或 FAILED/SENT；兼容旧失败任务无 Outbox 的情况。重复请求和成功任务无副作用，保持原成功快照。当前只提供后端接口与关联日志，不把它描述为完整运维平台。

**效果与证据。** 新增 3 项恢复集成测试，完整后端 64 项通过，见[回归报告](export-retry-regression.json)。备份本地库并重新打包部署，健康检查通过。经过前端代理，对成功任务重复调用两次未改变快照和时间，错误账本路径被拒，见[接口验收](export-retry-live-result.json)。失败恢复在隔离集成环境验证，没有在用户账本注入失败。

**剩余风险。** 尚无告警、持久化重试审计或前端按钮。排队恢复期间业务 FAILED 状态暂保留，需由消费更新；接口成功不等于任务完成。失败重试按执行时数据生成快照。历史 PENDING 无 Outbox 的任务、消费死信以及真正进程故障仍待专项处理。原任务加锁提供串行化设计，但本轮未新增跨实例并发重试实测。

**企业场景与学习。** 可靠性不仅是自动重试，还包括失败可发现、修复后能受控恢复。恢复路径必须遵守当前权限和事务规则；已成功的数据不能因为重投而被悄悄重算。

## E09 · 将发布失败与消费失败区分展示

**当时状态。** 受控重试已可用，但列表只提供业务状态。发布重试耗尽时业务可能仍是 PENDING，调用方无法区分正常等待与需要干预。

**问题与动机。** 单个状态无法表达异步链路的两个边界：消息成功发送不等于任务成功处理。盲目显示重试按钮还可能让用户在已排队时继续重复操作。

**采取方案。** 列表增加 deliveryStatus、publishAttempts、nextPublishAttemptAt、retryable。查询先限定当前成员发起的导出，再按授权 ID 分批读取 Outbox（每批 200），避免逐条读取。历史无 Outbox 数据明确标记 LEGACY；重试资格同时检查任务状态和当前权限，执行接口仍二次校验。

**效果与证据。** 新增状态演进与成员隔离集成用例，完整后端 65 项通过，见[报告](export-visibility-regression.json)。本地打包重启后，代理入口读取任务 12 返回 SENT + SUCCESS、发布 1 次、不可重试，见[联调](export-visibility-live-result.json)。本轮无数据库结构变化，未声称查询性能提升比例。

**剩余风险与取舍。** 接口已提供可见性，前端尚未消费新字段，告警尚未实现。发布次数人工恢复后清零，不能充当累计审计。导出列表尚未分页。消费重试耗尽的消息保留仍需死信方案；现有队列无死信参数，不能直接修改同名声明然后假设可无缝上线，需要明确迁移或策略配置及验证。

**企业场景与学习。** 异步任务状态应区分投递与执行；可重试是权限和状态共同决定的瞬时判断。批量查询减少逐条访问，但不是分页和容量验证的替代品。

## E10 · 消费重试耗尽后的消息保留

**当时状态。** 消费失败状态可查询、可受控重试，但原队列无死信参数，默认恢复策略不能保留失败消息供排查。

**问题与动机。** 任务表只能保存业务状态，不足以定位消费失败的原始消息；直接为现有同名队列增加声明参数会导致兼容性问题。

**采取方案与取舍。** 新增应用层恢复器与持久失败交换机/队列，名称由业务拓扑派生，不修改原队列。自动重试耗尽后转存原消息，检查发布确认与路由结果后才正常返回；转存失败抛出重新入队信号。保留 ID 与异常类型，不追加异常正文和堆栈。它属于失败消息保留，并非 broker DLX 改造。

**验证结果。** 编译阶段发现重新入队异常的包路径错误，按本地依赖修正后完成验证。新增真实消费者耗尽转存、不可路由恢复器失败测试；完整后端 67 项通过，见[报告](export-failure-retention-regression.json)。已打包重启本地服务，健康检查通过，原业务队列参数未变、新持久失败队列已存在，见[拓扑验收](export-failure-retention-live-result.json)。未对用户账本注入失败。

**边界与后续。** 转存至少一次，确认丢失可能重复留存；失败队列不可用会反复重试，需背压、暂停与告警。无效 JSON 等转换前毒消息尚待专项测试；无保留期限与持久审计。告警规则只是列出需求，尚未部署。新增其他消费者时需隔离恢复器用途。

**企业场景与学习。** 失败处理通道自身也会失败，不能将“发到死信队列”当作天然可靠。恢复策略要同时考虑确认、重复、积压、权限和既有拓扑兼容性。操作流程见[恢复说明](export-failure-retention.md)。

## E11 · 将故障状态变成可执行巡检

**当时状态。** 消费失败可保留、业务可恢复，但没有统一读取积压和停滞状态的入口。仅靠健康 HTTP 200 看不到业务任务是否停住。

**动机与方案。** 增加只读 Docker 巡检 CLI，汇总数据库与消息队列的 8 类问题，明确健康/待处理/未知以及对应退出码。每次命令有超时，不将依赖故障掩盖为健康；不读取消息正文，不改变队列。此阶段先提供可接入的检查契约，不虚构通知系统已上线。

**结果。** 6 项新脚本测试通过，并接入统一回归脚本；实际本地无失败/积压、消费者 1 个。故意指定不存在的容器返回 unknown/2，证据见[巡检报告](export-health-live-result.json)。Java 代码没有改变，本轮没有重复执行 67 项后端测试，其最近通过记录仍为 E10。

**边界与学习。** 默认 300 秒/100 条只是初始阈值；全表聚合代价、历史 FAILED 噪声和重复保留消息需要考虑。没有定时执行、通知、去重或告警恢复事件。监控本身也可能失败，必须提供未知状态；本地 root 兜底不应迁移到生产，生产应采用只读账号。

## E12 · 告警去重与恢复状态

**当时状态。** 巡检已有异常规则，但每次输出都是独立快照，直接接通知会反复提醒，探测失败还可能被误判为业务恢复。

**方案与实现。** 可选状态文件按监控目标隔离，记录 active 故障；首次 opened、持续去重、可靠消失 resolved。unknown 保留业务故障并单独记录探测故障。写入采用文件锁、临时文件 fsync 和原子替换；损坏、锁冲突或写失败返回未知而不覆盖原记录。

**结果。** 新增 7 项状态转换与持久化异常测试，脚本共 13 项通过。连续两次真实健康检查均无事件，见[验收](export-alert-state-result.json)。Java 未改动，本轮不重复宣称新的后端回归结果。

**边界与深化。** 不含调度和外部通知；最近事件不是持久事件日志，状态提交后 stdout 丢失可能造成通知漏发。后续通知应采用独立投递确认而非假设输出等于送达。当前写锁仅保护状态，采集应串行运行；无目录 fsync，不宣称断电持久化保证。实际状态文件应放持久目录，本地验收用了临时目录。

## E13 · Java 运行指标与访问隔离

**当时状态。** 外部巡检能看到导出积压，但 JVM、连接池和 HTTP 指标没有标准采集入口；JWT 拦截仅覆盖 /api/**，直接开放 Actuator 有风险。

**方案与实现。** 引入 Spring Boot Actuator、Micrometer Prometheus registry；管理监听独立绑定 127.0.0.1:9091，只允许 health/prometheus 且校验实际接收端口。增加导出 pending/dead/failed/最老等待和采集成功 gauge；共享 5 秒采样，无任务/用户标签。

**验证与发现。** 首轮完整 70 项通过。实际部署发现未开放端点被统一异常处理转换成 HTTP 200 错误体，增加安全层白名单拒绝。第二轮临时验证脚本复用 Redis 前缀、重建 MySQL 后出现 1 项幂等冲突；改每轮独立命名空间后完整 70 项通过。仓库统一脚本此前已有该隔离。见[回归报告](java-metrics-regression.json)。最终部署验证 JVM/Hikari/HTTP/导出指标存在，业务端口即使携带伪造转发头仍 403，env 在管理端口也 403，监听仅本机，见[实际验收](java-metrics-live-result.json)。

**边界与深化。** 没有部署 Prometheus server/Grafana/Alertmanager，没有外部通知。指标查询仍同步访问数据库，缓存只降低频率，不等于隔离慢查询；需要后续测试连接池占用和抓取耗时。本机隔离不是运维身份认证，多机采集需专门访问控制。没有高并发性能结论。详见[Java 可观测性](java-observability.md)。

## 下一阶段路线与验收门槛

| 顺序 | 需要深化的场景 | 具体产物 | 必须记录的效果 |
| --- | --- | --- | --- |
| 1 | Outbox 从代码走到运行闭环 | 迁移与启动、完整账本集成回归、导出联调、失败恢复操作说明 | 任务从创建到下载；故障后恢复；重复消息无重复业务效果；数据不被迁移破坏 |
| 2 | 环境一致性与回归 | 版本化迁移、隔离测试环境、CI；确定需要时引入 Flyway / Testcontainers | 干净环境可重复执行；记录命令、依赖版本、用例范围与报告 |
| 3 | 数据访问性能 | MyBatis SQL、分页、索引与批量查询；固定规模测试数据 | SQL 次数、EXPLAIN、扫描行数、P95/P99、错误率、CPU/内存/连接池，保留前后对照 |
| 4 | 外部地图慢、失败、超额 | 请求合并、全局配额、总预算超时、缓存与降级契约 | 外部调用数、命中率、超时率、恢复行为；不伪造不可用路线 |
| 5 | 图片私密访问与处理可靠性 | 对象存储权限、短期访问、异步状态；待算法提供后对接水印 | 越权拒绝、任务恢复；不同分辨率下质量、耗时和提取率 |
| 6 | 上线运行与容量边界 | 微信身份、指标告警、发布回滚与备份恢复；限流背压 | 故障发现时间、恢复过程、压测硬件/数据/并发模型和瓶颈；不只列 QPS |

智能规划保持轻量：意图路由 → 结构化规划草稿 → Java 校验约束 → 用户采纳与版本化保存。不得让模型直接绕过权限和事务修改账本。微服务、ES 等技术需有实际场景后再引入。

## 每阶段固定记录模板

后续每个有意义的交付追加一节，阶段未验收也要留下状态，不能等结束才补写结果。

```markdown
## E编号 · 阶段主题
- 日期 / 提交 / 负责人：
- 状态：调研 / 开发 / 测试通过 / 本地部署 / 验收完成（分别说明）
- 当时基线：已经有什么；缺少什么；运行版本与源码是否一致。
- 场景与问题：谁在什么操作下遇到什么；属于实际复现、测试发现还是风险推演。
- 动机与目标：不处理的后果；本阶段明确验收条件。
- 方案与取舍：选用机制、替代方案、复杂度与成本、正确性边界。
- 实现：业务规则、事务/并发、数据库、中间件、接口、迁移与回滚。
- 验证：环境、版本、数据量、故障注入、命令/报告链接、通过和失败范围。
- 实际效果：正确性结果；有测量才写性能数字；与预期收益分开。
- 剩余风险：未覆盖场景、依赖项、监控告警、人工恢复入口。
- 企业场景深化：这次学到的原理；上线如何发现与处理同类问题。
- 面试表达：场景 → 任务 → 行动 → 已有证据支持的结果；可追问的问题。
- 下一步：最优先工作与其验收门槛。
```

## 持续维护约定

1. 开发前先记录基线、问题分类和验收标准；开发中保留失败用例与方案变化的理由。
2. 开发后补齐提交、验证范围和剩余风险，同时更新交接文档的当前快照。代码完成、测试通过和部署验收分别记录。
3. 持久保留可复现的脚本、测试和脱敏报告；临时目录日志只辅助排查，不作为唯一证据。不得记录密钥、令牌或真实凭据正文。
4. 中断前写清未提交文件、最近一次通过的检查、未跑检查和下一条行动，降低恢复成本。
5. 简历可压缩成 4 条，但本记录不省略事实边界；预期收益不改写成已测数据。每条项目亮点都能追溯到阶段、代码与验收。
6. DDD 是业务建模与边界组织方法，TDD 是先失败测试再实现的开发循环。只对确实经历该循环的部分记录 TDD，不将「有测试」等同于全项目 TDD。
